## Цель разработки

Развиваем legacy GAS-фреймворк для Unity, ориентируясь на Unreal GAS и Lyra Starter Game.

Критерий готовности: legacy-код удалён, а весь функционал Unreal GAS/Lyra, релевантный проекту и применимый в Unity, реализован с сохранением поведения, жизненного цикла и семантики GAS.

## Документация источников

Используй источники в следующем порядке:

1. Семантика и принципы GAS — документация tranek:
   `C:\Users\NATALY\Documents\unity\lyra-starter-game-ue5\GASDocumentation-master`
   `README.md` и `AbilitySystemQuestions.md` - основные .md документации
2. Практическая архитектура и применение GAS — исходный код Lyra:
   `C:\Users\NATALY\Documents\unity\lyra-starter-game-ue5\Source`
3. Адаптация под C# и Unity с учётом ограничений платформы и явных архитектурных преимуществ.

Перед каждым крупным архитектурным шагом сверяй решение с документацией и Lyra. Значимые отклонения от референсов явно указывай и обосновывай.

Публичный API, основные типы сохраняй совместимыми с Unreal GAS. Нейминг идентичный GAS рефам. Внутреннюю реализацию буквально копировать не нужно; важны назначение API, жизненный цикл и семантика. Отклоняйся от оригинала только из-за ограничений Unity/C# или явного архитектурного преимущества.


## Наша документация

Файл документации:
`Assets\GAS\Documentation.md`

Документацию API ведёшь ты и самостоятельно вносишь изменения на русском языке.

Заполняй документацию постепенно, по мере реализации и проверки функциональности. За один шаг обновляй только разделы, непосредственно относящиеся к текущим изменениям. Не копируй всю документацию tranek заранее и не создавай пустые разделы-заглушки.

Для уже добавленных разделов сохраняй номера, порядок, назначение и структуру соответствующих разделов документации tranek. Адаптируй текст и примеры к нашей реализации на C# и Unity. Каждый добавленный раздел должен быть возможно буквально сопоставить с одноимённым разделом оригинальной документации.

Итоговая документация после завершения рефакторинга должна покрывать все применимые к проекту разделы документации tranek. Неприменимые разделы добавляй только при необходимости, кратко объясняя причину отклонения.

Обновляй документацию после логически завершённого этапа реализации, а не после каждой небольшой правки.


## Требования к коду

Используй адаптированный под Unity стек референса. Для асинхронного кода используй `com.cysharp.unitask`.

1. Не изменяй код проекта самостоятельно: предоставляй готовые атомарные блоки на уровне целого метода, группы свойств и тп., а правки вношу я. Старайся избегать неоднозначных правок, где не очевидно куда именно вставлять код.
3. Для каждого изменяемого или создаваемого файла указывай кликабельную ссылку с номером строки для открытия в VS Code.
4. Предлагай цельные логические правки без лишнего дробления. При изменении тела приводи метод целиком; если меняется только сигнатура — только её.
5. Для значимых методов добавляй английский XML `summary`: ровно одна полная строка без описаний параметров и свойств.
6. Строго соблюдай `.editorconfig`.
7. Высокоуровневый orchestration-код не должен дублировать `null`-проверки: их выполняет вызываемая низкоуровневая сущность.
8. По возможности выбирай производительные решения без ущерба корректности и ясности.
9. Удаляй ненужный код и другие «хвосты» как можно раньше.
10. В конце ответа кратко указывай результат и цель следующего шага. Общий процент готовности добавляй только при продолжительном рефакторинге.
11. Терминал не подключён; диагностические сообщения и ошибки проверяй в `Editor.log`.

## Форматирование

1. Не переноси выражение после =, если присваивание помещается в допустимую длину строки 90 символов; используй такой перенос только при необходимости.
2. Каждый аргумент в объявлении или вызове метода размещай на отдельной строке, кроме совсем абсурдных переносов одного короткого аргумента (.Instantiate(owner)).

## Архитектура

0. Самое важное правило всего рефактора.
> Перед добавлением любого типа, метода, свойства или поля сначала найди аналог в Unreal GAS и сохрани его нейминг, назначение и жизненный цикл, то есть весь оригинальный pipeline. Прикрепляй обязательно ссылки на оригинальную документацию api от Epic Games или tranek. Если для имплементации чего-либо требуется оригинальная сущность, которой у нас нет, то сначала имплементируем эту сущность. Собственное API вводи только при отсутствии аналога или ограничении Unity/C#, явно фиксируя отклонение.

1. Не использовать события как скрытый control flow для обязательных этапов операции. Основной жизненный цикл должен выражаться явными вызовами методов; события предназначены для необязательных наблюдателей и уведомлений, отсутствие которых не меняет корректность операции.

2. Долгоживущие подписки реализуй по disposable-модели:
регистрация возвращает подписку с `IDisposable` и `IsDisposed`;
владелец освобождает подписку через `Dispose()`;
`Dispose()` должен быть идемпотентным за счёт состояния `IsDisposed`;
прямые += и -= допустимы только внутри реализации подписки или при ограничениях Unity/Mirror;
внутри GAS использовать готовую реализацию `DisposableSubscription`, `DisposableGroup`.

3. Для новых сериализуемых свойств без дополнительной логики используй auto-property с `[field: SerializeField]` вместо отдельного backing field. Явное поле используй, только если требуется собственная логика доступа, отдельное управление сериализованным состоянием или миграция существующих данных.

## Tests

тестировать внутреннее копирование отдельно не нужно. Перестраиваем тесты как acceptance-сценарии: один тест выдаёт способность, активирует её штатным GAS-пайплайном и проверяет конечное состояние мира. Внутренние Spec, handle и transport DTO остаются деталями реализации.